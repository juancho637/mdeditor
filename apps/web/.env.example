# Browser URLs — leave empty for auto-detection (works in Docker + host).
# Next.js rewrites proxy /api/* and /collaboration/* to the backend server-side.
# Set explicitly for production: https://your-domain.com / wss://your-domain.com
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=